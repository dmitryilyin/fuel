Install keystone service on the controller node
===============================================

This task installs Keystone service on the controller node
with Simple configuration. Requires database already set up
and user created.
